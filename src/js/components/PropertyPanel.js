import React, { Component, PropTypes } from 'react';

import './PropertyPanel.sass'

const PropertyPanel = ()=> (
  <div className="property-panel">property panel</div>
)

export default PropertyPanel
